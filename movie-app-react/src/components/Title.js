import React from 'react';

const Title = (props) => {
    return(
        <div>
            <h1>This is the title:</h1>
            <p>{props.title}</p>
        </div>
       
    )
}

export default Title;